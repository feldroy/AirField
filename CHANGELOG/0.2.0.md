## AirField 0.2.0

AirField extends `pydantic.Field` so models can declare which field is the primary key. [AirModel](https://github.com/feldroy/AirModel) already depends on it for its async PostgreSQL ORM.

```sh
uv add AirField
```

### What's new

- **AirField function** exported as `from airfield import AirField`. Accepts all the same arguments as `pydantic.Field` plus `primary_key`, stored in `json_schema_extra`.

### Contributors

[@audreyfeldroy](https://github.com/audreyfeldroy) (Audrey M. Roy Greenfeld) designed and built this release.
